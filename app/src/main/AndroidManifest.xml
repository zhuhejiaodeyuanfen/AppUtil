<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vivian.apputil">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- 写入sdk -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 读sdk -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- 联系人权限 -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".AppApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".view.HomeActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.PermissionActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".view.WeChatActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".view.MyCameraActivity" />
        <activity
            android:name=".view.FullscreenCameraActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".view.PhotoListActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".view.photo.PhotoDetailViewActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".view.photo.SelectPhotoActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".view.photo.PhotoEditActivity" />
        <activity android:name=".view.VideoPlayChoiceActivity" />
        <activity android:name=".view.ContactListActivity"></activity>
    </application>

</manifest>